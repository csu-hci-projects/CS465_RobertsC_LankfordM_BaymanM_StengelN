using System.Collections;
using System.Collections.Generic;
using UnityEngine.Events;
//using Tobii.G2OM;
using UnityEngine;


public class CountFixations : MonoBehaviour
{
/*
    public class CountFixations : MonoBehaviour, IGazeFocusable
    {
        
        public int counter;

        private BoxCollider _collider;

        void Start()
        {
            _collider = this.gameObject.GetComponent<BoxCollider>();
        }

        public void GazeFocusChanged(bool hasFocus)
        {
            if (hasFocus)
            {
                counter++;
            }
            else
            {

            }
        }


        public void resetCounter()
        {
            counter = 0;
        }


        public void setColliderSize()
        {

            if (GlobalVars.Instance.thisObjectShape == GlobalVars.ObjectShape.line)
            {

                if (GlobalVars.Instance.thisDrawnSize == GlobalVars.DrawnSize.small)
                {
                    _collider.size = new Vector3(0.028f, 0.028f, 0.028f);
                }
                if (GlobalVars.Instance.thisDrawnSize == GlobalVars.DrawnSize.medium)
                {
                    _collider.size = new Vector3(0.07f, 0.07f, 0.07f);
                }
                if (GlobalVars.Instance.thisDrawnSize == GlobalVars.DrawnSize.large)
                {
                    _collider.size = new Vector3(0.13f, 0.13f, 0.13f);
                }
            }
            else
            {
                if (GlobalVars.Instance.thisDrawnSize == GlobalVars.DrawnSize.small)
                {
                    _collider.size = new Vector3(0.072f, 0.03f, 0.05f);
                }
                if (GlobalVars.Instance.thisDrawnSize == GlobalVars.DrawnSize.medium)
                {
                    _collider.size = new Vector3(0.2f, 0.075f, 0.05f);
                }
                if (GlobalVars.Instance.thisDrawnSize == GlobalVars.DrawnSize.large)
                {
                    _collider.size = new Vector3(0.36f, 0.075f, 0.05f);
                }

            }

        }
        */
    }
